name: Deploy to GitHub Pages

on:
  push:
    branches: [ main, develop ]
    paths:
      - '**.js'
      - '**.html'
      - '**.css'
      - 'package.json'
      - 'src/**.grammar'
      - '!page/page-en/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
      
      - name: Generate Lezer Parser
        run: npx lezer-generator src/wikidot.grammar -o src/parser.js
      
      - name: Build project
        run: npm run build
        
      - name: Prepare Public Directory
        # Always publish to repository root, avoid deploy-to-/dev behavior on develop
        run: |
          mkdir -p public

          # 1. Copy dist artifacts first (if present)
          if [ -d "dist" ]; then
            cp -r dist/* public/
          fi

          # 2. Ensure entry pages are at site root
          [ -f "index.html" ] && cp index.html public/
          [ -f "test.html" ] && cp test.html public/

          # 3. Copy static assets
          if [ -d "assets" ]; then
            mkdir -p public/assets
            cp -r assets/* public/assets/
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          destination_dir: .
          keep_files: true
